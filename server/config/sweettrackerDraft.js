// ── 스마트택배 로그인 문의 자동 초안 설정 ──
//
// 답변 내용을 바꾸고 싶으면 이 파일만 수정하면 된다.
// 수동 실행 전용이라 서버 재시작은 필요 없다.
//
// 분기 규칙(rules): 문의 내용에서 keywords 가 먼저 매칭되는 규칙의 템플릿을
// 사용하고, 아무 규칙에도 해당하지 않으면 기본 템플릿(defaultRule)을 쓴다.
// 규칙별로 intro(첫 안내 문장)만 다르게 하거나, body 전체를 지정해 완전히
// 다른 답변을 쓸 수도 있다.

// 공통 본문 — intro 문장만 규칙별로 갈아끼운다
const baseBody = (intro) => `안녕하세요.
스마트 택배입니다.

${intro}

쇼핑몰 로그인 기능은 각 쇼핑몰의 공식 연동 서비스와는 별도로 제공되는 부가 기능으로 자동 로그인 방식과 직접 로그인 방식을 제공하고 있습니다.

- 자동 로그인 방식은 스마트택배 앱에 아이디와 비밀번호를 입력하면 해당 정보로 로그인 시도를 지원하는 방식입니다. 다만, SNS 로그인, 이중 인증, 보안문자, 캡차(CAPTCHA) 등 추가 인증 또는 보안 절차가 필요한 경우에는 로그인 지원이 어렵습니다.

- 직접 로그인 방식은 스마트택배 앱 내에서 쇼핑몰 로그인 화면을 제공하여 사용자가 해당 화면에서 직접 로그인하는 방식입니다. 로그인 후에는 세션을 통해 로그인을 유지하고 있으며, 세션이 만료되면 다시 로그인이 필요합니다.

안내드린 방법으로 참고하셔서 이용부탁드립니다.

감사합니다.`;

module.exports = {
  // 초안을 만들 메일함의 소유자 이메일 (이 앱에 Google 로그인한 계정).
  // 스마트택배 문의 메일은 help@blumn.ai 메일함으로 들어오므로 그 계정을 쓴다.
  // ※ help@blumn.ai 계정으로 이 앱에 Google 로그인이 1회 되어 있어야 한다.
  ownerEmail: process.env.SWEETTRACKER_OWNER_EMAIL || "help@blumn.ai",

  // Gmail 검색 조건 — 스마트택배 문의처리 라벨의 미읽음 메일
  gmailQuery:
    'label:스마트택배-문의처리 is:unread subject:"스마트택배 문의가 들어왔습니다"',

  // 이 라벨이 이미 붙은 메일은 처리 완료로 보고 건너뛴다.
  // gmail.modify 스코프가 있으면 처리 후 이 라벨을 붙이고, 없으면
  // DB(sweettracker_draft_logs) 기록만으로 중복을 막는다.
  processedLabelName: "스마트택배 자동답변완료",

  // 한 번 실행에 처리할 최대 메일 수 (폭주 방지)
  maxPerRun: 20,

  // 문의 내용에 이 단어들 중 하나라도 있어야 "로그인 문의"로 본다
  loginKeywords: ["로그인", "로그 인", "자동로그인", "세션 만료", "세션만료"],

  // ── 쇼핑몰별 분기 규칙 ── ※ 답변을 다르게 하고 싶은 쇼핑몰을 여기에 추가
  // body 를 직접 지정하면 완전히 다른 답변을 쓸 수 있다:
  //   { name: "쿠팡", keywords: ["쿠팡"], subject: "...", body: `전용 답변 전문` }
  rules: [
    {
      name: "쿠팡",
      keywords: ["쿠팡"],
      subject: "[스마트택배] 쿠팡 로그인 문의 답변",
      body: baseBody("문의해 주신 쿠팡 로그인 관련 내용에 대해 답변드립니다."),
    },
    {
      name: "지마켓",
      keywords: ["지마켓", "G마켓", "g마켓", "gmarket"],
      subject: "[스마트택배] 지마켓 로그인 문의 답변",
      body: baseBody("문의해 주신 지마켓 로그인 관련 내용에 대해 답변드립니다."),
    },
    {
      name: "카카오",
      keywords: ["카카오"],
      subject: "[스마트택배] 카카오 계정 로그인 문의 답변",
      body: baseBody("문의해 주신 카카오 계정 로그인 관련 내용에 대해 답변드립니다."),
    },
  ],

  // 어느 규칙에도 해당하지 않는 로그인 문의에 쓰는 기본 답변
  defaultRule: {
    name: "기본",
    subject: "[스마트택배] 로그인 문의 답변",
    body: baseBody("문의해 주신 내용에 대해 답변드립니다."),
  },
};
